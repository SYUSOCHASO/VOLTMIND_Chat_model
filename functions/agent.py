from functions.funs import gemini_chat

def VOLTMINDBOT(user_inputs, system_prompt):
    res = gemini_chat(user_inputs, system_prompt)
    return res

def 税務GPT(user_inputs, system_prompt):
    res = gemini_chat(user_inputs, system_prompt)
    return res

def 薬科GPT(user_inputs, system_prompt):
    res = gemini_chat(user_inputs, system_prompt)
    return res

def 敬語の鬼(user_inputs, system_prompt):
    res = gemini_chat(user_inputs, system_prompt)
    return res

def 節税商品説明AI(user_inputs, system_prompt):
    res = gemini_chat(user_inputs, system_prompt)
    return res

def IT用語説明AI(user_inputs, system_prompt):
    res = gemini_chat(user_inputs, system_prompt)
    return res

def 要件定義書のヒアリングAI(user_inputs, system_prompt):
    res = gemini_chat(user_inputs, system_prompt)
    return res

def ビジネス向け要件定義書(user_inputs, system_prompt):
    res = gemini_chat(user_inputs, system_prompt)
    return res

def エンジニア向け要件定義書(user_inputs, system_prompt):
    res = gemini_chat(user_inputs, system_prompt)
    return res

def 金額提示相談AI(user_inputs, system_prompt):
    res = gemini_chat(user_inputs, system_prompt)
    return res
